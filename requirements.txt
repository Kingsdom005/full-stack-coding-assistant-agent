# 全栈代码助手智能体 - Python 依赖清单
# Python 版本: 3.13+ (推荐 3.13.x 或 3.11.x)

# ==================== 核心依赖 ====================
# LiteLLM - 统一大模型接口 (支持 Python 3.13)
litellm>=1.0.0

# python-dotenv - 从 .env 文件读取环境变量 (支持 Python 3.13)
python-dotenv>=1.0.0

# PyYAML - YAML 配置文件解析 (支持 Python 3.13)
pyyaml>=6.0

# ==================== 可选依赖（根据需要取消注释） ====================
# 如果需要 Web API 接口
# fastapi>=0.100.0
# uvicorn>=0.20.0

# 如果需要高级日志
# loguru>=0.7.0

# 如果需要数据验证
# pydantic>=2.0.0

# 如果需要 HTTP 客户端
# requests>=2.31.0
# httpx>=0.27.0
